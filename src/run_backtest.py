import json
from datetime import datetime, timedelta, timezone
from src.backtest import Backtester

if __name__ == "__main__":
    b = Backtester()
    # Simple runner for backtest verification output
    # ... logic handled mainly in tests/
